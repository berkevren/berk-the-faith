package card;

public class NormalMonsterCard extends MonsterCard {

    public NormalMonsterCard(String cardName, String cardText, CardTypes cardType, String attribute,
                             String monsterType, int attackPoints, int defencePoints, int level) {
        super(cardName, cardText, cardType, attribute, monsterType, attackPoints, defencePoints, level);
    }
}
